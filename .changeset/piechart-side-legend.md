---
'@industry/mobile': patch
---

`PieChart`: novas props `legendPlacement` (`'bottom' | 'right'`), `legendValue` (`'valueAndPercent' | 'percent'`), `centerLabel` (conteúdo centralizado sobre o furo do donut) e `testID`. Os padrões preservam o comportamento atual.
